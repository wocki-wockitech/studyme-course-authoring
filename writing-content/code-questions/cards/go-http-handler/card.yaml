id: f1a2b3c4-5555-4444-aaaa-000000000005
difficulty: 3
tags: [go, http, handlers, json]

questions:
  - type: code_fill
    text:
      ru: "Допишите HTTP handler чтобы он возвращал JSON:"
      en: "Complete the HTTP handler to return JSON:"
    file: handler.go
    slots:
      - answer: ['"application/json"', '"application/json; charset=utf-8"']
        hints:
          - { ru: "MIME-тип для JSON", en: "MIME type for JSON" }
      - answer: ["WriteHeader(200)", "WriteHeader(http.StatusOK)"]
        hints:
          - { ru: "Метод для установки HTTP status code", en: "Method to set HTTP status code" }
          - { ru: "WriteHeader с кодом 200", en: "WriteHeader with code 200" }

  - type: true_false
    text:
      ru: "Content-Type для JSON ответа — text/plain."
      en: "Content-Type for a JSON response is text/plain."
    correct: false
